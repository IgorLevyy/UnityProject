using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testForCapsule : MonoBehaviour
{
    public int A;
    // Start is called before the first frame update
    void Start()
    {
        A = 5;
    }

    // Update is called once per frame
    void Update()
    {
        A++;
    }
}
